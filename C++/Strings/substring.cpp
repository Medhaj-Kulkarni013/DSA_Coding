#include<bits/stdc++.h>
using namespace std;
int main(){
    string s = "abcdef";
    cout<<s.substr(3)<<endl; // from 3rd index to end will be shown
    // s.substr(idx,len)
    cout<<s.substr(2,3);
}