#include<stdio.h>
int main(){
    int arr[5] = {5,6,7,8,9};
    printf("%d ",arr[0]); // 0 1 2 3 4 are index for each value of array
    printf("%d ",arr[1]);// indexes always start from 0 to sizeOfarr-1
    printf("%d ",arr[2]);// index is like a roll no. for each value of arr.
    printf("%d ",arr[3]);
    printf("%d ",arr[4]);
    return 0;
}